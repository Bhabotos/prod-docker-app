# Production Ready Dockerized Web Application

FastAPI + PostgreSQL + Redis + Nginx, containerized with Docker Compose, deployed via CI/CD to a production VPS with SSL.

## Status

Work in progress — built incrementally, milestone by milestone.
