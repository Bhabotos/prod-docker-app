# Python
__pycache__/
*.pyc
.venv/
venv/

# Environment secrets
.env
.env.*
!.env.example
!.env.prod.example

# Docker
*.log

# Editors/OS
.vscode/
.idea/
.DS_Store

# Postgres/pgAdmin local data (if ever bind-mounted instead of named volume)
pgdata/
pgadmin_data/

# Generated at deploy time by scripts/init_ssl.sh — templates are committed,
# the rendered file (with the real domain baked in) is not.
nginx/nginx.prod.conf

# Local Postgres backups (see scripts/backup.sh)
backups/
