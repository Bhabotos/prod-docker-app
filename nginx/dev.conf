# Local development / CI: plain HTTP on any hostname.
server {
    listen 80;
    server_name _;

    location = /nginx-health { access_log off; default_type text/plain; return 200 "ok\n"; }

    include /etc/nginx/snippets/app_locations.conf;
}
