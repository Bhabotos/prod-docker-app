"""BMI & Health Dashboard: a self-contained feature package.

Importing this package must stay dependency-free (no FastAPI, no database) so
that `python -m bmi_dashboard.cli` works with the standard library only.
"""
